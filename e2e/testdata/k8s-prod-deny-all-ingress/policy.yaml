apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: prod
spec:
  # Empty podSelector selects every pod in the namespace.
  podSelector: {}
  # Ingress in policyTypes with no ingress rules => deny all ingress.
  policyTypes:
    - Ingress
