assertions:
  - name: same-namespace source allowed
    from: default/client
    to: default/web
    expect: allow
  - name: other-namespace source denied
    from: secondary/client
    to: default/web
    expect: deny
